

export default function SectionDescription({ description }: { description: string }) {
    return (
        <p className='md:text-center md:w-[80%] text-[22px] font-[spectral] text-[#94a3b8] mb-5  mx-auto'>{description}</p>
    )
}
