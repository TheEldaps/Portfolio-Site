

export default function SectionHeader({ title }: { title: string }) {
    return (
        <h2 className='text-[#F8FAC] font-[DMSerif] text-3xl lg:text-5xl mb-5 pt-5 md:text-center'>{title}</h2>
    )
}
